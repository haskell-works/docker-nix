machine:
  services:
    - docker

dependencies:
  cache_directories:
  pre:
    - docker login -u $DOCKER_USER -p $DOCKER_PASS -e $DOCKER_EMAIL
  override:
    - docker build -t "haskell-works/docker-nix:$CIRCLE_BRANCH" .
    - sleep 100000
